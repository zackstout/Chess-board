
class Knight extends Piece {
  constructor(x, y, col) {
    super();
    this.color = col;
    this.pos = {
      x: x,
      y: y
    };
  }

  // just need to check if on board and also in a certain set of 8 cells.


}
